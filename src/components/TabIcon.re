let tintColor = Theme.Colors.highlight;
open ReactNative;
[@react.component]
let make = (~name, ~size=20, ~color=tintColor) => {
  <View>
     <FontAwesome name size color /> </View>;
    //   selected ? Styles.theme.palette.primary : Styles.theme.palette.grey;
    // let color =
};
